SELECT
  username
FROM
  users;

SELECT
  COUNT(*)
FROM
  posts;

SELECT
  COUNT(*)
FROM
  likes;

SELECT
  COUNT(*)
FROM
  followers;

SELECT
  COUNT(*)
FROM
  caption_tags;

SELECT
  COUNT(*)
FROM
  comments;

SELECT
  COUNT(*)
FROM
  hashtags;

SELECT
  COUNT(*)
FROM
  hashtags_posts;

SELECT
  COUNT(*)
FROM
  photo_tags;